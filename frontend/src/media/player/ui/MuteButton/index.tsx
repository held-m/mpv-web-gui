import { usePlayerState } from "@src/lib/zustand/player";

// export const MuteButton = () => {
//   const { muted, toggleMuted } = usePlayerState();
//
//   return (
//     <button onClick={toggleMuted}>
//       {muted ? <VolumeOffIcon /> : <VolumeUpIcon />}
//     </button>
//   );
// };
